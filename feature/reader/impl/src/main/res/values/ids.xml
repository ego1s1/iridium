<resources>
    <!-- Stable container id so the FragmentManager can restore the Readium
         navigator fragment across configuration changes. -->
    <item name="reader_navigator_container" type="id" />
</resources>
