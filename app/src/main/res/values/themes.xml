<resources>
    <!-- AppCompat parent: the Readium navigator inflates AppCompat widgets. -->
    <style name="Theme.Iridium" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>
</resources>
